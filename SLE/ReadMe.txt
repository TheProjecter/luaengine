1、所有函数、类名以大写字母开头的表示只在库内部使用
2、所有函数、类名全部小写的都是public的
3、ZLuaEngine.cpp中包含了部分测试代码
4、StdAfx.h, StdAfx.cpp中定义了一些库中使用的宏
5、注释中的"TODO:"表示此处以后要修改

更多功能开发中：
1、支持userdata
2、在lua中注册c++函数和对象
3、函数支持多返回值
4、错误处理

改进：
PushMethod已经可以返回false但调用的函数还没有做检测